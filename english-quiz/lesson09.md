# 第9回：点数を数えよう

## 今日のゴール 🎯
何問正解したか点数を数えて、みんなに見せる！

## 学ぶこと 📚
- 点数を覚えさせておく変数
- 数を増やす計算（`+ 1`）
- 文字と数字をくっつける方法

## やってみよう！ 💻

### ステップ1：スコアを保存する変数を作ろう
```javascript
let score = 0; // 最初は0点
let totalQuestions = 0; // 何問解いたか
```

### ステップ2：正解の時に点数を増やそう
```javascript
function checkAnswer(choice) {
  if (choice === currentAnswer) {
    score = score + 1;
    alert("正解！ 今の点数: " + score);
  }
  // 次の問題へ
  setQuestion();
}
```

## 理解度チェック ✅
1. 中身を後から変えることができる変数の箱を作るキーワードは何ですか？
2. 数の数え始め（初期値）として、一番よく使われる数字は何ですか？
3. `score = score + 1` を、もっと短く書く方法は何ですか？
4. 命令の中で、文字と数字を「くっつける」ときに使う記号は何ですか？
5. スコアを表示するための `alert("今の点数: " + score)` という命令で、もし `score` が 10 だった場合、画面には何と表示されますか？
